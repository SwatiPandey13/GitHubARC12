curl: (22) The requested URL returned error: 404
Error: building at STEP "RUN export ARCH=$(echo ${TARGETPLATFORM} | cut -d / -f2)     && if [ "$ARCH" = "amd64" ] || [ "$ARCH" = "x86_64" ] || [ "$ARCH" = "i386" ]; then export ARCH=x64 ; fi     && mkdir -p "$RUNNER_ASSETS_DIR"     && cd "$RUNNER_ASSETS_DIR"     && curl -fLo runner.tar.gz https://github.com/actions/runner/releases/download/v${RUNNER_VERSION}/actions-runner-linux-${ARCH}-${RUNNER_VERSION}.tar.gz     && tar xzf ./runner.tar.gz     && rm runner.tar.gz     && mv ./externals ./externalstmp": while running runtime: exit status 22
